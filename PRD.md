# PU-SignUpBot 产品需求文档（PRD）

## 文档信息

| 项目 | 内容 |
|------|------|
| 项目名称 | PU-SignUpBot（PU 口袋校园报名助手） |
| 文档版本 | v2.0.0 |
| 当前状态 | 重构中 — CLI → GUI |
| 目标用户 | 需要抢报 PU 口袋校园活动的在校大学生 |

---

## 1. 产品背景

PU-SignUpBot 是一款帮助大学生自动报名 PU 口袋校园活动的工具。目前为命令行（CLI）版本，使用门槛较高，非计算机专业的同学操作困难。本次重构目标是将其改造为带图形界面（GUI）的桌面应用，双击即用，降低使用门槛。

### 1.1 现存痛点

| 痛点 | 说明 |
|------|------|
| 命令行操作 | 普通学生不会用终端，畏惧黑框 |
| 交互繁琐 | 每一步都需要手动输入 y/n、索引号，容易出错 |
| 信息不直观 | 活动列表以文字逐条打印，无法一览全貌 |
| 进度不可见 | 报名过程中只有文字日志，不知道当前状态 |
| 安装复杂 | 需要安装 Python + pip + 依赖，对非专业用户极不友好 |

### 1.2 重构目标

- 用 CustomTkinter 构建美观实用的 GUI
- 用 PyInstaller 打包为单文件 exe，双击即用
- 保留多账号并行报名核心能力
- 移除邮件通知功能（简化流程）

---

## 2. 技术选型

| 维度 | 选择 | 说明 |
|------|------|------|
| GUI 框架 | CustomTkinter | 基于 Tkinter 的现代化封装，Material Design 风格，打包体积小（~30MB） |
| Python 版本 | ≥3.11 | 使用 uv 管理虚拟环境 |
| 打包工具 | PyInstaller | 单文件 `--onefile` 模式，配置 `--windowed` 隐藏控制台 |
| 架构模式 | 事件驱动 MVC | `core/` 纯后端逻辑，`ui/` 纯前端展示，后端绝不引用前端 |

---

## 3. 功能范围

### 3.1 保留功能

| 功能 | 描述 |
|------|------|
| 多账号管理 | 添加、删除 PU 账号（最多 6 个），账号按顺序逐个报名 |
| 登录认证 | 用户名/密码登录获取 Token，自动刷新过期 Token |
| 学校匹配 | 输入学校名称，模糊搜索匹配学校 ID（SID） |
| 活动筛选 | 按年级、活动分类、归属院系筛选活动 |
| 活动浏览 | 列出符合条件的活动（名称、分类、举办组织、分数、可报名人数） |
| 选择活动 | 从列表中勾选想要报名的活动 |
| 自动报名 | 三段式多线程抢报名策略 |
| 时间同步 | 与服务器对时，精准卡点报名 |
| 活动监控 | 每 1 小时轮询活动开始时间，防止主办方临时修改；用户可手动刷新 |
| 日志记录 | 按天写入日志文件（WARNING 以上），GUI 内实时显示全部日志 |

### 3.2 移除功能

| 功能 | 移除原因 |
|------|---------|
| 邮件通知 | 流程繁琐，非核心功能，学生可直接在 App 内查看结果 |
| CLI 交互式问答 | 全部由 GUI 表单和按钮替代 |

### 3.3 新增功能

| 功能 | 描述 |
|------|------|
| 可视化用户管理 | 卡片式展示所有用户，一目了然 |
| 实时报名进度 | 进度条 + 每活动状态表格 + 彩色日志列表 |
| 操作中止 | 报名过程中可随时中止所有任务，立即强杀线程 |
| 监控持久化 | 活动监控状态写入 `settings.json`，应用崩溃重启后自动恢复未完成的监控任务 |
| 暗色模式 | 支持亮色/暗色主题切换，使用 `customtkinter.set_appearance_mode()` |

## 4. 项目结构

```
PU-SignUpBot/
├── main.py                        # GUI 入口（重写）
├── pyproject.toml                 # 依赖管理（修改）
├── requirements.txt               # pip 兼容，由 pyproject.toml 自动导出
├── user_data.json                 # 用户数据（修改：移除 email 字段）
├── settings.json                  # 全局设置 + 监控持久化（新增）
├── PRD.md                         # 本文件
├── README.md                      # 使用文档
│
├── core/                          # 纯后端逻辑，禁止引用任何 GUI 库
│   ├── __init__.py
│   ├── headers.py                 # HTTP 请求头配置（不变）
│   ├── pu_sign.py                 # AES 加密 / X-Sign 签名（不变）
│   ├── PUExceptions.py            # 自定义异常（不变）
│   ├── activity_bot.py            # 报名机器人核心（修改：移除邮件）
│   ├── single.py                  # 单用户报名调度（修改：移除邮件 + 添加 callback）
│   ├── tools.py                   # 工具函数集（大幅修改：移除邮件 + 去除交互式 I/O）
│   └── user_data_manager.py       # 用户数据管理（修改：添加 CRUD，移除交互式方法）
│
├── ui/                            # 全部 GUI 代码，可引用 core/
│   ├── __init__.py
│   ├── app.py                     # 主窗口、侧边导航、页面路由、日志管道
│   ├── styles.py                  # 主题常量（颜色、字体、圆角、间距）
│   ├── widgets/
│   │   ├── __init__.py
│   │   ├── log_widget.py          # 实时彩色日志控件
│   │   └── user_card.py           # 用户信息卡片组件
│   └── pages/
│       ├── __init__.py
│       ├── dashboard_page.py      # 首页：用户概览
│       ├── add_user_page.py       # 添加用户（多步骤向导）
│       ├── activity_select_page.py # 活动筛选与选择
│       └── signup_page.py         # 报名监控 + 实时日志
│
└── utils/                         # 整个目录删除
```

---

## 5. GUI 页面设计

### 5.1 主窗口布局

```
┌──────────────────────────────────────────────────────────┐
│ PU-SignUpBot - PU 口袋校园报名助手         [_] [□] [X]  │
├────────────┬─────────────────────────────────────────────┤
│            │                                             │
│  首页      │           内容区域                          │
│  添加用户  │           （点击左侧导航切换页面）           │
│  选择活动  │                                             │
│  开始报名  │                                             │
│            │                                             │
│            │                                             │
├────────────┴─────────────────────────────────────────────┤
│ 状态栏    │ 用户: 3  │  活动: 12  │  报名中: 1           │
└──────────────────────────────────────────────────────────┘
```

### 5.2 首页（Dashboard）
·
**页面定位**：总览所有用户，一站式操作入口。

**布局**：

```
┌────────────────────────────────────────────────┐
│  [＋ 添加用户]                    [▶ 全部报名] │
├────────────────────────────────────────────────┤
│ ┌──────────────────────┐ ┌───────────────────┐ │
│ │ 253202304218         │ │ 253202304240      │ │
│ │ 法学院               │ │ 法学院            │ │
│ │ ● Token 有效         │ │ ● Token 有效      │ │
│ │ 已选活动: 5          │ │ 已选活动: 0       │ │
│ │ [删除] [选活动]        │ │ [删除] [选活动]    │ │
│ └──────────────────────┘ └───────────────────┘ │
│ ┌──────────────────────┐                        │
│ │ ...                  │                        │
│ └──────────────────────┘                        │
└────────────────────────────────────────────────┘
```

### 5.3 添加用户页（Add User）

**页面定位**：多步骤向导式添加新 PU 账号。

**步骤流程**：

```
步骤 1：输入凭证
  ┌──────────────────────────────┐
  │ 用户名（学号） [            ] │
  │ 密码          [            ] │
  │                              │
  │              [下一步]        │
  └──────────────────────────────┘

步骤 2：选择学校
  ┌──────────────────────────────┐
  │ 学校关键词    [     ] [搜索] │
  │ 匹配结果      [▼下拉列表]    │
  │                              │
  │    [上一步]     [下一步]     │
  └──────────────────────────────┘

步骤 3：补充信息
  ┌──────────────────────────────┐
  │ 院系全称      [            ] │
  │   （如：经济管理学院）       │
  │                              │
  │    [上一步]   [验证并保存]   │
  └──────────────────────────────┘
```

### 5.4 选择活动页（Activity Select）

**页面定位**：浏览和筛选可报名的活动，勾选想要报名的。

**筛选逻辑**：同级维度内为 OR（勾选法学院 + 计算机学院 → 两个学院的活动都显示），跨维度为 AND（年级=2023 AND 分类=学术讲座 AND 院系=法学院）。UI 中需有提示文字说明此规则。

**布局**：

```
┌──────────────┬──────────────────────────────────────┐
│ 选择用户      │ [▼ 253202304218]                    │
├──────────────┼──────────────────────────────────────┤
│ 筛选条件      │       [获取活动]                     │
│              ├──────────────────────────────────────┤
│ ▼ 参与年级    │ 共找到 12 个活动                     │
│   ☑ 2023     │ ┌──────────────────────────────────┐ │
│   ☐ 2024     │ │ ☑ 学术讲座A  │ 2分 │ 法学院     │ │
│   ☐ 2025     │ │ ☐ 文体活动B  │ 1分 │ 计算机学院 │ │
│ ▼ 活动分类    │ │ ☑ 志愿服务C  │ 3分 │ 经济学院   │ │
│   ☑ 学术讲座  │ │ ...                              │ │
│   ☐ 文体活动  │ │                                  │ │
│ ▼ 归属院系    │ │ 已选: 3 个                       │ │
│   ☑ 法学院    │ │ [保存选择]                       │ │
│   ☐ 计算…     │ └──────────────────────────────────┘ │
└──────────────┴──────────────────────────────────────┘
```

### 5.5 报名监控页（Signup）

**页面定位**：启动报名后，实时展示整体进度、每个活动的状态和日志。

**布局**：

```
┌────────────────────────────────────────────────────┐
│ 总体进度                                           │
│ [████████████░░░░░░░░░░░░░░░░] 45%                 │
│ 总计: 12  │  成功: 5  │  失败: 1  │  等待: 6      │
├────────────────────────────────────────────────────┤
│ 活动状态表格                                       │
│ 用户           │ 活动ID  │ 状态       │ 时间       │
│ ───────────────┼─────────┼───────────┼─────────── │
│ 253202304218   │ 12345   │ ✅ 成功   │ 10:00:01   │
│ 253202304218   │ 12346   │ ⏳ 等待   │ 14:00:00   │
│ 253202304240   │ 12347   │ 👁 监控中 │ 15:00:00   │
│ 253202304240   │ 12348   │ ❌ 失败   │ 10:00:02   │
├────────────────────────────────────────────────────┤
│ [▶ 全部开始] [⏹ 中止]                             │
├────────────────────────────────────────────────────┤
│ 实时日志                                           │
│ 10:00:00 │ INFO    │ 用户 253202304218 登录成功    │
│ 10:00:01 │ SUCCESS │ 用户 253202304218 报名成功！  │
│ 10:00:02 │ ERROR   │ 用户 253202304240 报名失败    │
│ 10:00:03 │ WARNING │ 活动 12347 时间已变更         │
└────────────────────────────────────────────────────┘
```

---

## 6. 架构设计

### 6.1 分层规则

```
┌─────────────────────┐
│       ui/           │  → 可 import core/
│    (GUI 界面)       │
├─────────────────────┤
│       core/         │  → 禁止 import ui/ 或任何 GUI 库
│    (纯后端逻辑)     │
└─────────────────────┘
```

### 6.2 线程模型

| 线程类型 | 触发时机 | 持续时间 | 如何通知 GUI |
|---------|---------|---------|-------------|
| 网络请求 | 登录、获取活动 | 秒级 | 函数返回到主线程回调 |
| 活动获取 | 遍历分页获取活动 | 秒~分钟 | 函数返回到主线程回调 |
| 报名监控 | `_monitor_start_time` | 分钟~小时 | callback 参数 |

**监控持久化机制**：
- 启动监控时，将监控任务（activity_id、userName、start_time）写入 `settings.json` 的 `pending_monitors` 数组
- 报名完成或用户中止后，清除对应条目
- 应用启动时检查 `pending_monitors`，若有残留条目则自动恢复监控线程
| 报名请求 | `_start_signup_threads` | 秒级 | callback 参数 |

**核心原则**：
- 工作线程绝不直接操作 GUI 控件，所有 UI 更新通过 `window.after(0, func)` 调度到主线程。
- 账号报名为串行模式：一个账号的所有活动报名结束后，自动切换下一个账号。

### 6.3 日志方案

```
工作线程 → loguru → QueueSink → queue.Queue
                                      │
GUI 主线程 ← after(250ms) 轮询 ←────┘
    │
    └→ LogWidget.append_log(level, message)
        按级别着色：INFO(白) WARNING(黄) ERROR(红) SUCCESS(绿)
```

---

## 7. 代码变更清单

### 7.1 新增文件

| 文件 | 描述 |
|------|------|
| `core/` 目录及 `__init__.py` | 后端包 |
| `ui/` 目录及 `__init__.py` | UI 包 |
| `ui/app.py` | 主窗口、导航、路由、日志管道 |
| `ui/styles.py` | 主题常量 |
| `ui/widgets/log_widget.py` | 彩色日志控件 |
| `ui/widgets/user_card.py` | 用户信息卡片 |
| `ui/pages/dashboard_page.py` | 首页 |
| `ui/pages/add_user_page.py` | 添加用户页 |
| `ui/pages/activity_select_page.py` | 活动选择页 |
| `ui/pages/signup_page.py` | 报名监控页 |
| `settings.json` | 全局设置 + 监控持久化任务队列 |

### 7.2 修改文件

| 文件 | 改动内容 |
|------|---------|
| `main.py` | 完全重写，改为 GUI 入口 |
| `core/activity_bot.py` | 移除 `_send_email_notification()`、`_send_fail_email_notification()`、`self.email`；`print()` → `logger.debug()`；添加 callback 参数 |
| `core/tools.py` | 移除 `send_email()`、`make_success_email()`、`make_fail_email()`；将 `input()/print()` 分离为纯数据函数 |
| `core/user_data_manager.py` | 移除 `add_new_user()`、`process_user()`；添加 `add_user()`、`remove_user()`、`update_user()`、`get_user()` |
| `core/single.py` | 移除邮件引用；透传 callback 参数 |
| `pyproject.toml` | 添加 `customtkinter>=5.2.2` |
| `requirements.txt` | 删除，由 `pip-compile pyproject.toml` 或 `uv export` 自动生成 |
| `user_data.json` | 移除每个用户对象的 `email` 字段 |

### 7.3 删除文件

| 文件 | 原因 |
|------|------|
| `config.py` | 唯一内容 ENABLE_EMAIL_NOTIFICATION 不再需要 |
| `utils/`（整个目录） | 所有文件迁移到 `core/` |

---

## 8. 打包方案

### 8.1 打包命令

```bash
pyinstaller --onefile --windowed \
    --name "PU-SignUpBot" \
    --add-data "core;core" \
    --add-data "ui;ui" \
    --hidden-import customtkinter \
    --hidden-import PIL._tkinter_finder \
    --hidden-import Crypto.Cipher \
    --hidden-import Crypto.Util.Padding \
    --hidden-import Crypto.Random \
    --exclude-module email \
    --exclude-module smtplib \
    main.py
```

### 8.2 打包产物

- 单文件 `dist/PU-SignUpBot.exe`
- 预期体积：~30-40 MB（含 Python 运行时 + 所有依赖）
- 运行要求：Windows 10+，无需安装 Python

---

## 9. 验收标准

| 编号 | 验收项 | 通过条件 |
|------|--------|---------|
| AC-1 | 架构隔离 | `grep -r "customtkinter\|from ui\|import ui" core/` 返回空 |
| AC-2 | 添加用户 | 多步骤向导完成用户添加，user_data.json 正确写入 |
| AC-3 | 获取活动 | 筛选条件后能正确获取并展示活动列表 |
| AC-4 | 选择活动 | 勾选活动后保存，user_data.json 中 activity_ids 正确更新 |
| AC-5 | 自动报名 | 到时间后自动启动多线程报名，成功/失败状态正确显示 |
| AC-6 | GUI 响应 | 报名过程中拖动窗口、切换页面不卡顿 |
| AC-7 | 实时日志 | 日志在 GUI 内实时滚动显示，颜色正确 |
| AC-8 | 中止操作 | 点击中止后报名线程在合理时间内停止 |
| AC-9 | 错误处理 | 断网、密码错误等异常有友好提示，不崩溃 |
| AC-10 | exe 运行 | 打包后的 exe 在无 Python 环境的 Windows 上双击正常运行 |
| AC-11 | 账号串行 | 账号按顺序逐个报名，前一个完成后自动切换下一个 |
| AC-12 | Token 过期恢复 | Token 过期后自动刷新重试，不影响报名流程 |
| AC-13 | 监控恢复 | 应用重启后自动检测 `pending_monitors` 并恢复未完成的监控任务 |

---

## 10. 风险与注意事项

| 风险 | 影响 | 应对措施 |
|------|------|---------|
| CustomTkinter 控件不够丰富 | 部分交互可能无法实现 | 必要时用原生 Tkinter 控件补充 |
| 多线程 + GUI 线程安全问题 | 界面崩溃、数据显示错乱 | 所有 UI 更新统一用 `after()` 调度到主线程 |
| PyInstaller 打包遗漏依赖 | exe 运行报错 | 提前测试所有隐藏导入，尤其是 Crypto 子模块 |
| 不同分辨率/缩放适配 | 显示变形 | 使用相对布局，设置最小窗口尺寸 |
| Token 过期 | 报名中途失败 | 保留原有重试/刷新逻辑，异常时在 GUI 提示 |
| exe 无代码签名 | Windows SmartScreen 拦截，显示"此应用可能危害您的设备" | README 中说明绕过步骤，用户首次运行时手动放行 |

---

> **文档状态**：待评审
> **最后更新**：2026-05-09
